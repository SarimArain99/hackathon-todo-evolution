# Better Auth Configuration
# REQUIRED: Generate a secure secret with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-key-here-generate-with-openssl-rand-base64-32

# Better Auth URL (auto-detected for Vercel, set explicitly for local)
BETTER_AUTH_URL=http://localhost:3000

# Trusted origins for CSRF protection (comma-separated list)
# Include all domains where your app will be accessed
BETTER_AUTH_TRUSTED_ORIGINS=http://localhost:3000,https://zenith-flow-zeta.vercel.app

# API Configuration (Backend FastAPI)
NEXT_PUBLIC_API_URL=http://localhost:8000

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Email Verification (set to "true" to enable)
REQUIRE_EMAIL_VERIFICATION=false

# Credential Persistence Configuration
# Database connection string for PostgreSQL (Neon recommended)
DATABASE_URL=postgresql://user:password@host:5432/database

# Email Service (Resend) for password reset and email verification
# Get your API key from https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxxxxx

# Sender email for password reset and verification emails
# Must be verified in your Resend dashboard
RESEND_FROM_EMAIL=noreply@yourdomain.com

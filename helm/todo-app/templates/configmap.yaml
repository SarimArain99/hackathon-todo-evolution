apiVersion: v1
kind: ConfigMap
metadata:
  name: todo-config
  labels:
    {{- include "todo-app.labels" . | nindent 4 }}
data:
  # Non-sensitive configuration (FR-009)
  NEXT_PUBLIC_API_URL: {{ .Values.frontend.env.NEXT_PUBLIC_API_URL | quote }}
